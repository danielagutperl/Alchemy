import React, { Fragment } from 'react'
import SearchContainer from './SearchContainer.js'

const Home = () => (
  <section>
    <div className="hero">
      <h1>
        Alchemy
      </h1>
      <SearchContainer />
    </div>
  </section>
)

export default Home
